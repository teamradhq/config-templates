# TRHQ Configurations

A set of templates for configuring various tools like PHPStan, PHPUnit, etc.

## Usage

Install the package:

```bash
composer require --dev teamradhq/config-templates
```

List available templates and commands:

```bash
vendor/bin/trhq.config list:configs
```
